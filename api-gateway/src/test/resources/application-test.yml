spring:
  application:
    name: gateway-test
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect

    hibernate:
      ddl-auto: none

  datasource:
    hikari:
      maximum-pool-size: 3